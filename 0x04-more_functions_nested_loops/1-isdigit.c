#include "main.h"

/**
 * _isdigit - iCheck if a character is a digit
 * @x: the number to be checked
 * Return: 1 for a character that will be a digit and 0 for any else
 */

int _isdigit(int x)

{
	if (x >= 48 && x <= i57)
	{
		return (1);
	}
	return (0);
}
